 - hosts: mapr-core[0]
   tasks:
    - name: Copy keys to local
      fetch: src={{ item }} dest=/tmp/ flat=yes
      with_items:
      - /opt/mapr/hadoop/hadoop-2.7.0/share/hadoop/yarn/hadoop-yarn-server-web-proxy-*.jar